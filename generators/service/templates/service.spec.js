describe('<%= _.camelCase(name) %>', function() {

  beforeEach(module('<%= appname %>'));

  it('should ...', inject(function(<%= _.camelCase(name) %>) {

    //expect(<%= _.camelCase(name) %>.doSomething()).toEqual('something');

  }));
});